interface LeafletProps {
  
}

export function Leaflet({  }: LeafletProps) {
  return (
    <div className="w-full h-full flex flex-col items-center justify-center">
        Leaflet
    </div>
  );
}
